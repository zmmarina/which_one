spring.application.name=which_one

# Disable all Micrometer metrics
management.metrics.enable-all=false
management.tracing.enabled=false

# Disable Datadog auto-configuration explicitly
spring.autoconfigure.exclude=org.springframework.boot.actuate.autoconfigure.metrics.export.datadog.DatadogMetricsExportAutoConfiguration
